inputs:
  flag_ninja:
    default: false
    type: boolean

runs:
  using: composite
  steps:
  - name: make build
    shell: bash
    working-directory: ./scripts
    run: bash ./make_build.sh ${{inputs.flag_ninja}}