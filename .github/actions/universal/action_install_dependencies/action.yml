inputs:
  flag_ninja:
    default: false
    type: boolean
  cmake_version:
    type: string
    
runs:
  using: composite
  steps:
  - if: ${{inputs.flag_ninja}}
    run: |
      sudo apt-get update
      sudo apt-get -y -qq install ${{inputs.cmake_version}} ninja-build
    shell: bash
    