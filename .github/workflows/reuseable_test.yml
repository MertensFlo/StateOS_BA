name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
  repository_dispatch:
  schedule:
    - cron: '0 0 1 * *'

jobs:
  call-build:
    uses: MertensFlo/StateOS_BA/.github/workflows/build_test.yml@Testing_workflows
    with:
      name: test1

  call-example:
    uses: ./.github/workflows/pipeline_elements/test_test.yml

  call-static-code-test:
    uses: ./.github/workflows/pipeline_elements/static_code_test.yml

  call-unit-test:
    uses: ./.github/workflows/pipeline_elements/unit_test.yml
